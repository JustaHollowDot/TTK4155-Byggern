#ifndef BYGGERN_PING_PONG_OLED_H
#define BYGGERN_PING_PONG_OLED_H

struct OLed {
    // 128 * 64 bit screen addressable in 8 bit chunks
    // 2*x circular buffers containing current frame and next frame
    // boolean telling which buffer to display
};

#endif //BYGGERN_PING_PONG_OLED_H
